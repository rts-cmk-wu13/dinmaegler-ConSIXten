import React from 'react';
import HomeCards from '../Cards/HomeCards'; // Adjusted path to Cards component

export default function HPsection() {
    return (
        <section className="hp-section">
            <HomeCards />
           
        </section>
    );
}